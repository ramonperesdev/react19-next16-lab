name: Deploy to Production

# Só executa quando há push na branch main
on:
  push:
    branches: [main]

env:
  NODE_VERSION: 20

jobs:
  # Primeiro garante que todos os testes passam
  ci-checks:
    name: 🔍 Pre-Deploy Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Instalar dependências
        run: npm ci

      - name: Lint, Type Check e Testes
        run: |
          npm run lint
          npm run type-check
          npm run test:ci

      - name: Build
        run: npm run build

  # Deploy para produção
  deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    needs: ci-checks
    environment:
      name: production
      url: https://react19-next16-lab.vercel.app # Substitua pela URL real

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Deploy para produção
        run: |
          echo "🚀 Deploy iniciado..."
          echo "📦 Enviando build para servidor..."
          echo "✅ Deploy concluído com sucesso!"

          # AQUI SERIA O COMANDO PARA DEPLOY NA AWS AMPLIFY
          # - AWS Amplify: aws amplify ...

      - name: Notificar sucesso
        if: success()
        run: echo "✅ Deploy para produção realizado com sucesso!"

      - name: Notificar falha
        if: failure()
        run: echo "❌ Deploy falhou! Verificar logs."
