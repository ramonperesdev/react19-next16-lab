version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm run type-check
        - npm run test:ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
